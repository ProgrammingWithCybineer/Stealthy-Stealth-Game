
init python:

    class Item:

        def __init__(self, name):
            self.name = name

    def add_item(item):

        player.inventory.append(item)

    def remove_item(item):

        player.inventory.remove(item)